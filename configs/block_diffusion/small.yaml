inherits: ../base.yaml
variant: block_diffusion
device: mps
model:
  attn_type: block_causal
  use_time_cond: true
  block_size: 16
  max_length: 128
train:
  epochs: 2000
  steps: null
  batch_size: 28
  lr: 0.0003
data:
  train_size: null
  val_size: null
  overfit_text: null
eval:
  every_n_epochs: 1
viz:
  every_n_steps: 50
